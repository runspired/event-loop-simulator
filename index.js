// requestAnimationFrame
function requestAnimationFrame(cb) {}

// user timers
function setTimeout(cb, ms) {}

// promises
function resolve(cb) {}

// idle callbacks
function requestIdleCallback(cb) {}

// window.postMessage | MessageChannel.postMessage
// localStorage events | indexdb events | SharedChannel
function postMessage(cb) {}

// mutation observers
function mutationObserver(cb) {}

// IntersectionObserver
// Scroll
function behaviorObserver(cb) {}

// click etc. | fetch | ajax
function userEvent(cb) {}
